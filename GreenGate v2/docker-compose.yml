version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: sustaingate-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sustaingate
      POSTGRES_PASSWORD: sustaingate
      POSTGRES_DB: sustaingate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sustaingate"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: sustaingate-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
